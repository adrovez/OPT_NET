export const environment = {
  production: true,
  // En despliegue real, reemplazar por la URL pública de OPT.API (o servir tras un
  // reverse proxy que exponga el backend bajo el mismo origen en la ruta /api).
  apiUrl: '/api',
};
